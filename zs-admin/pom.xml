<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <parent>
        <groupId>com.zs</groupId>
        <artifactId>zs-project-java</artifactId>
        <version>1.0.0</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>zs-admin</artifactId>
    <description>系统模块</description>
    <packaging>jar</packaging>


    <dependencies>



        <!-- 代码生成 -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-generator</artifactId>
        </dependency>


        <!-- 权限 -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-common-security</artifactId>
        </dependency>

        <!-- 邮件 -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-mail-service</artifactId>
        </dependency>

        <!-- 系统 -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-system-service</artifactId>
        </dependency>

        <!-- 文件 -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-file-service</artifactId>
        </dependency>

        <!-- 短信 -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-sms-service</artifactId>
        </dependency>

        <!-- websocket -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-websocket-service</artifactId>
        </dependency>

        <!-- bpm -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-bpm-service</artifactId>
        </dependency>

        <!-- 基础 -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-infra-service</artifactId>
        </dependency>

        <!-- 业务 -->
        <dependency>
            <groupId>com.zs</groupId>
            <artifactId>zs-business-service</artifactId>
        </dependency>



    </dependencies>

    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <mainClass>com.zs.ZsAdminApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
            </plugin>
        </plugins>
    </build>


</project>
