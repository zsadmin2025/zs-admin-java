<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.gen.mapper.GenTableMapper">



	<select id="selectTableListByNames" resultType="com.zs.gen.domain.entity.GenTable">
		select table_name, table_comment, create_time, update_time from information_schema.tables
		where table_schema = (select database())
        <if test="tables != null and tables.size > 0">
              and table_name in
            <foreach collection="tables" item="name" open="(" separator="," close=")">
                #{name}
            </foreach>
         </if>
	</select>
    <select id="dbPage" resultType="com.zs.gen.domain.entity.GenTable">
        select table_name, table_comment, create_time, update_time from information_schema.tables
        where table_schema = (select database())    AND table_name NOT REGEXP '(?i)(jimu|gen|qrtz)'
        <if test="ew != null and ew.sqlSegment != null">
            ${ew.sqlSegment}
        </if>
    </select>
</mapper>