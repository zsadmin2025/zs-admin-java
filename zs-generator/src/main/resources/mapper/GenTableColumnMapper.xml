<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.zs.gen.mapper.GenTableColumnMapper">


	<sql id="selectGenTableColumnVO">
        select column_id, table_id, column_name, column_comment, column_type, java_type, java_field, is_pk, is_increment, is_required, is_insert, is_edit, is_list, is_query, query_type, html_type, dict_type, sort, create_by, create_time, update_by, update_time from gen_table_column
    </sql>

    <select id="selectGenTableColumnListByTableId" resultType="com.zs.gen.domain.entity.GenTableColumn">
        <include refid="selectGenTableColumnVO"/>
        where table_id = #{tableId}
        order by sort
    </select>

    <select id="selectTableColumnsByName" resultType="com.zs.gen.domain.entity.GenTableColumn">
		select column_name,
		       (case when is_nullable = 'no' then '1' else '0' end) as is_required,
		       (case when column_key = 'PRI' then '1' else '0' end) as is_pk,
		       ordinal_position as sort,
		       column_comment,
		       (case when extra = 'auto_increment' then '1' else '0' end) as is_increment,
		       column_type
		from information_schema.columns where table_schema = (select database()) and table_name = (#{tableName})
		order by ordinal_position
	</select>


    <delete id="deleteGenTableColumnByIds" parameterType="Long">
        delete from gen_table_column where table_id in
        <foreach collection="array" item="tableId" open="(" separator="," close=")">
            #{tableId}
        </foreach>
    </delete>

    <delete id="deleteGenTableColumns">
        delete from gen_table_column where column_id in
        <foreach collection="list" item="item" open="(" separator="," close=")">
            #{item.columnId}
        </foreach>
    </delete>

</mapper>